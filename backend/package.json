{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "build": "npx prisma migrate deploy --schema=prisma/schema.prisma && npx prisma generate --schema=prisma/schema.prisma && tsc"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "@vercel/node": "^2.15.10",
    "dotenv": "^16.5.0",
    "openai": "^5.0.1",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "prisma": "^6.8.2",
    "typescript": "^5.8.3"
  }
}
